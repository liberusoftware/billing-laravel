<div><form wire:submit="createCapability"><input type="text" wire:model="type" placeholder="{{ __('Type') }}"><input type="text" wire:model="name" placeholder="{{ __('Name') }}"><button type="submit">{{ __('Create') }}</button></form>@if (session('isp-capabilities-message'))<p role="status">{{ session('isp-capabilities-message') }}</p>@endif<ul wire:loading.class="opacity-50"><li wire:loading>{{ __('Loading…') }}</li>@forelse ($capabilities as $capability)<li wire:key="isp-capability-{{ $capability->id }}">{{ $capability->type }}: {{ $capability->name }} ({{ $capability->status }}) <button type="button" wire:click="$set('selectedCapabilityId', {{ $capability->id }})">{{ __('Select') }}</button></li>@empty<li>{{ __('No ISP capabilities found.') }}</li>@endforelse</ul>@if ($selectedCapabilityId)<form wire:submit="transitionCapability"><select wire:model="status"><option value="pending">{{ __('Pending') }}</option><option value="active">{{ __('Active') }}</option><option value="suspended">{{ __('Suspended') }}</option><option value="cancelled">{{ __('Cancelled') }}</option><option value="failed">{{ __('Failed') }}</option></select><button type="submit">{{ __('Update status') }}</button></form>@endif</div>
