<div><form wire:submit="provision"><input type="text" wire:model="number" placeholder="{{ __('Number') }}"><input type="text" wire:model="type" placeholder="{{ __('Type') }}"><button type="submit">{{ __('Provision') }}</button></form>@if (session('billing-communications-message'))<p role="status">{{ session('billing-communications-message') }}</p>@endif<ul wire:loading.class="opacity-50"><li wire:loading>{{ __('Loading…') }}</li>@forelse ($numbers as $number)<li wire:key="communication-number-{{ $number->id }}">{{ $number->number }} ({{ $number->status }}) <button type="button" wire:click="$set('selectedNumberId', {{ $number->id }})">{{ __('Select') }}</button></li>@empty<li>{{ __('No communication numbers found.') }}</li>@endforelse</ul>@if ($selectedNumberId)<form wire:submit="transition"><select wire:model="status"><option value="available">{{ __('Available') }}</option><option value="active">{{ __('Active') }}</option><option value="suspended">{{ __('Suspended') }}</option><option value="released">{{ __('Released') }}</option><option value="failed">{{ __('Failed') }}</option></select><button type="submit">{{ __('Update status') }}</button></form>@endif</div>
