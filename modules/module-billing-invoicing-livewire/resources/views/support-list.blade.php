<section aria-labelledby="module-billing-invoicing-support-heading">
    <h2 id="module-billing-invoicing-support-heading">{{ __('Invoice support') }}</h2>
    @if (session('module-billing-invoicing-support-message'))<p role="status">{{ session('module-billing-invoicing-support-message') }}</p>@endif
    <label>{{ __('Invoice') }} <select wire:model="selectedInvoiceId"><option value="">{{ __('Select an invoice') }}</option>@foreach ($invoices as $invoice)<option value="{{ $invoice->id }}">{{ $invoice->number }}</option>@endforeach</select></label>
    <form wire:submit="addLine"><h3>{{ __('Add invoice line') }}</h3><label>{{ __('Description') }} <input wire:model="description" required></label><label>{{ __('Quantity') }} <input wire:model="quantity" type="number" min="1" required></label><label>{{ __('Unit amount') }} <input wire:model="unitAmountMinor" type="number" min="0" required></label><label>{{ __('Tax rate') }} <input wire:model="taxRate" type="number" min="0" max="100" step="0.01" required></label><button type="submit">{{ __('Add line') }}</button></form>
    <form wire:submit="createSupport"><h3>{{ __('Create support record') }}</h3><label>{{ __('Type') }} <select wire:model="supportType"><option value="tax">{{ __('Tax') }}</option><option value="credit">{{ __('Credit') }}</option><option value="adjustment">{{ __('Adjustment') }}</option><option value="pdf">{{ __('PDF') }}</option><option value="delivery">{{ __('Delivery') }}</option></select></label><label>{{ __('Amount') }} <input wire:model="supportAmountMinor" type="number" min="0" required></label><label>{{ __('Currency') }} <input wire:model="supportCurrency" maxlength="3"></label><label>{{ __('Destination') }} <input wire:model="supportDestination"></label><button type="submit">{{ __('Create support') }}</button></form>
    <ul wire:loading.class="opacity-50">@forelse ($items as $item)<li wire:key="invoice-support-{{ $item->id }}">{{ $item->type }} for invoice {{ $item->invoice_id }} ({{ $item->status }})</li>@empty<li>{{ __('No invoice support records found.') }}</li>@endforelse</ul>
</section>
