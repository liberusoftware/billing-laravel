<section aria-labelledby="module-billing-invoicing-payment-plans-heading"><h2 id="module-billing-invoicing-payment-plans-heading">{{ __('Payment plans') }}</h2>@if(session()->has('module-billing-invoicing-payment-plans-message'))<p role="status">{{ session('module-billing-invoicing-payment-plans-message') }}</p>@endif<form wire:submit="createPlan"><label>{{ __('Invoice') }} <select wire:model="invoiceId" required><option value="">{{ __('Select invoice') }}</option>@foreach($invoices as $invoice)<option value="{{ $invoice->id }}">{{ $invoice->number ?: $invoice->id }} — {{ $invoice->total_minor }} {{ $invoice->currency }}</option>@endforeach</select></label><label>{{ __('Installments') }} <input type="number" min="2" wire:model="totalInstallments" required /></label><label>{{ __('Frequency') }} <select wire:model="frequency"><option value="weekly">{{ __('Weekly') }}</option><option value="monthly">{{ __('Monthly') }}</option><option value="quarterly">{{ __('Quarterly') }}</option></select></label><button type="submit">{{ __('Create payment plan') }}</button></form><label>{{ __('Plan to run') }} <select wire:model="selectedPlanId"><option value="">{{ __('Select plan') }}</option>@foreach($plans as $plan)<option value="{{ $plan->id }}">{{ $plan->invoice_id }} — {{ $plan->generated_installments }}/{{ $plan->total_installments }}</option>@endforeach</select></label><button type="button" wire:click="runPlan">{{ __('Create installment') }}</button></section>
