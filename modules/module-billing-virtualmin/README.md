# Billing Virtualmin

Optional Virtualmin GPL/Pro adapter for `module-billing-hosting`. It registers
`virtualmin` and `virtualmin-pro` drivers for account lifecycle, plan, and addon
operations through the Virtualmin remote API.
