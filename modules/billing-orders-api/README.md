# Liberu Billing Orders API

Versioned Sanctum API adapter for the billing-orders domain module.

The package exposes the team-scoped order collection and checkout endpoints
under /api/v1/billing/orders. Domain validation and authorization remain in
the core package. See openapi/v1/billing-orders.yaml for the contract.
