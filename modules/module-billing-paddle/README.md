# Billing Paddle

Paddle Billing adapter for the provider-neutral `liberusoftware/module-billing-payments` contract.

Install `liberusoftware/billing-paddle`, enable `module-billing-paddle`, and configure `PADDLE_API_KEY` and optionally `PADDLE_API_URL`. Payment metadata supplies `paddle_price_id`; partial refunds additionally require `paddle_transaction_item_id`.
