<section aria-labelledby="billing-customer-portal-billing-heading"><h2 id="billing-customer-portal-billing-heading">{{ __('Customer billing') }}</h2><label>{{ __('Customer ID') }} <input type="number" min="1" wire:model.live="customerId" /></label><label>{{ __('From') }} <input type="date" wire:model.live="startAt" /></label><label>{{ __('To') }} <input type="date" wire:model.live="endAt" /></label>@if($customerId)<dl>@foreach($billing['status'] as $label => $amount)<dt>{{ str_replace('_', ' ', ucfirst($label)) }}</dt><dd>{{ $amount }}</dd>@endforeach</dl><h3>{{ __('Billing history') }}</h3><ul>@foreach($billing['history'] as $invoice)<li>{{ $invoice['invoice_number'] ?: $invoice['date'] }} — {{ $invoice['amount_minor'] }} {{ $invoice['currency'] }} — {{ $invoice['status'] }}</li>@endforeach</ul><h3>{{ __('Payment trends') }}</h3><ul>@foreach($billing['trends'] as $trend)<li>{{ $trend['month'] }} — {{ $trend['total_paid'] }}</li>@endforeach</ul>@endif</section>
