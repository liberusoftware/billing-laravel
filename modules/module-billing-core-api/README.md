# Billing Core API

The optional HTTP adapter for Billing Core. It exposes only the documented
account collection and creation operations, uses Sanctum authentication, and
delegates persistence and lifecycle rules to the core package.
