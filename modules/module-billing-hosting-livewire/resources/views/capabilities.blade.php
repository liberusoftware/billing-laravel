<div><form wire:submit="createCapability"><input type="text" wire:model="type" placeholder="{{ __('Type') }}"><input type="text" wire:model="name" placeholder="{{ __('Name') }}"><button type="submit">{{ __('Create') }}</button></form>@if (session('hosting-capabilities-message'))<p role="status">{{ session('hosting-capabilities-message') }}</p>@endif<label>{{ __('Status') }} <select wire:model="status"><option value="pending">Pending</option><option value="active">Active</option><option value="suspended">Suspended</option><option value="cancelled">Cancelled</option><option value="failed">Failed</option></select></label><ul wire:loading.class="opacity-50"><li wire:loading>{{ __('Loading…') }}</li>@forelse ($capabilities as $capability)<li wire:key="hosting-capability-{{ $capability->id }}">{{ $capability->type }}: {{ $capability->name }} ({{ $capability->status }}) <button type="button" wire:click="transition({{ $capability->id }})">{{ __('Update status') }}</button></li>@empty<li>{{ __('No hosting capabilities found.') }}</li>@endforelse</ul></div>
