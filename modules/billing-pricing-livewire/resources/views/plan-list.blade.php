<section aria-labelledby="billing-pricing-heading"><h2 id="billing-pricing-heading">{{ __('Pricing plans') }}</h2>@if(session()->has('billing-pricing-message'))<p role="status">{{ session('billing-pricing-message') }}</p>@endif<button type="button" wire:click="$set('showCreate', true)">{{ __('Create pricing plan') }}</button>@if($showCreate)<form wire:submit="save"><label>{{ __('Name') }} <input wire:model="name" required /></label><label>{{ __('Amount in minor units') }} <input type="number" min="0" wire:model="unitAmountMinor" required /></label><button type="submit">{{ __('Save') }}</button></form>@endif<ul>@forelse($plans as $plan)<li>{{ $plan->name }} — {{ $plan->unit_amount_minor }} {{ $plan->currency }} ({{ $plan->pricing_model->value }})</li>@empty<li>{{ __('No pricing plans found.') }}</li>@endforelse</ul></section>
