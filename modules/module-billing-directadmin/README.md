# Billing DirectAdmin

Optional DirectAdmin adapter for `module-billing-hosting`. It registers the
`directadmin` driver and implements account lifecycle, package, and addon
operations through DirectAdmin's API.
