# Billing ResellerClub

Optional ResellerClub adapter for the provider-neutral Billing Domains registrar contract. Configure `services.resellerclub.auth_userid`, `api_key`, `reseller_id`, and optionally `base_url`.
