# Liberu Billing Orders Livewire

Livewire 4 adapter for the billing-orders domain module.

Render the component with the stable package alias
module-billing-orders::order-list. Component state is validated at the
boundary and mutations delegate to the core order action.
